import { SelectedLocation } from './selected-location.model';

describe('SecectedLocation', () => {
  it('should create an instance', () => {
    expect(new SelectedLocation()).toBeTruthy();
  });
});
